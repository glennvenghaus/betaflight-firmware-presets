#$ TITLE: fpvapnea - Quad - Battery specific settings (alerts/thr/rth)
#$ FIRMWARE_VERSION: 4.4
#$ CATEGORY: OTHER
#$ STATUS: EXPERIMENTAL
#$ KEYWORDS: defaults, master, build, bat, gps, rth
#$ AUTHOR: gvapnea
#$ DESCRIPTION: Quad / Bat specific set alerts, throttle mid (in rate 0 and 1) and gps rescue hover point
#$ PRIORITY: 0

#$ OPTION BEGIN (UNCHECKED): Cinebot30/GP9 - GNB1300HV 4S
    # -- ADDED: Cinebot30/GP9 - GNB1300HV 4S --

	# failsafe
	rxfail 3 s 1550

	# gps
	set gps_rescue_throttle_min = 1350
	set gps_rescue_throttle_hover = 1546
	set gps_rescue_throttle_max = 1900

	# voltage
	set vbat_max_cell_voltage = 440
	set vbat_warning_cell_voltage = 345
	set vbat_min_cell_voltage = 335

	# capacity
	set bat_capacity = 1300
	set osd_cap_alarm = 1000

	# rate
	rateprofile 0
	set thr_mid = 55
	rateprofile 1
	set thr_mid = 55

#$ OPTION END

#$ OPTION BEGIN (UNCHECKED): Cinebot30/GP9 - GNB1500HV 4S
    # -- ADDED: Cinebot30/GP9 - GNB1500HV 4S --

	# failsafe
	rxfail 3 s 1575

	# gps
	set gps_rescue_throttle_min = 1400
	set gps_rescue_throttle_hover = 1561
	set gps_rescue_throttle_max = 1750

	# voltage
	set vbat_max_cell_voltage = 440
	set vbat_warning_cell_voltage = 345
	set vbat_min_cell_voltage = 335

	# capacity
	set bat_capacity = 1500
	set osd_cap_alarm = 1250

	# rate
	rateprofile 0
	set thr_mid = 56
	rateprofile 1
	set thr_mid = 56

#$ OPTION END

#$ OPTION BEGIN (UNCHECKED): Cinebot30/GP9 - GNB1350 4S
    # -- ADDED: Cinebot30/GP9 - GNB1350 4S --

	# failsafe
	rxfail 3 s 1550

	# gps
	set gps_rescue_throttle_min = 1400
	set gps_rescue_throttle_hover = 1556
	set gps_rescue_throttle_max = 1750

	# voltage
	set vbat_max_cell_voltage = 430
	set vbat_warning_cell_voltage = 345
	set vbat_min_cell_voltage = 335

	# capacity
	set bat_capacity = 1350
	set osd_cap_alarm = 1000

	# rate
	rateprofile 0
	set thr_mid = 56
	rateprofile 1
	set thr_mid = 56

#$ OPTION END

#$ OPTION BEGIN (UNCHECKED): Cinebot30/no cam - FLYWOO900HV 4S - 5 blades
    # -- ADDED: Cinebot30/no cam - FLYWOO900HV 4S -- 5 blades

	# failsafe
	rxfail 3 s 1300

	# gps
	set gps_rescue_throttle_min = 1300
	set gps_rescue_throttle_hover = 1295
	set gps_rescue_throttle_max = 1650

	# voltage
	set vbat_max_cell_voltage = 440
	set vbat_warning_cell_voltage = 345
	set vbat_min_cell_voltage = 335

	# capacity
	set bat_capacity = 900
	set osd_cap_alarm = 700

	# rate
	rateprofile 0
	set thr_mid = 30
	rateprofile 1
	set thr_mid = 30

#$ OPTION END


#$ OPTION BEGIN (UNCHECKED): LR Explorer/GP9 - FLYWOO900HV 4S
    # -- ADDED: LR Explorer/GP9 - FLYWOO900HV 4S --

	# failsafe
	rxfail 3 s 1475

	# gps
	set gps_rescue_throttle_min =1350
	set gps_rescue_throttle_hover = 1453
	set gps_rescue_throttle_max = 1700

	# voltage
	set vbat_max_cell_voltage = 440
	set vbat_warning_cell_voltage = 345
	set vbat_min_cell_voltage = 335

	# scale
	set vbat_scale = 111
	set ibata_scale = 212

	# capacity
	set bat_capacity = 900
	set osd_cap_alarm = 700

	# rate
	rateprofile 0
	set thr_mid = 46
	rateprofile 1
	set thr_mid = 46

#$ OPTION END

#$ OPTION BEGIN (UNCHECKED): LR Explorer/GP9 - FLYWOO3000LION 4S
    # -- ADDED: LR Explorer/GP9 - FLYWOO3000LION 4S --

	# failsafe
	rxfail 3 s 1700

	# gps
	set gps_rescue_throttle_min = 1500
	set gps_rescue_throttle_hover = 1689
	set gps_rescue_throttle_max = 1850

	# voltage
	set vbat_max_cell_voltage = 422
	set vbat_warning_cell_voltage = 300
	set vbat_min_cell_voltage = 280

	# scale
	set vbat_scale = 111
	set ibata_scale = 196

	# capacity
	set bat_capacity = 2800
	set osd_cap_alarm = 2100

	# rate
	rateprofile 0
	set thr_mid = 69
	rateprofile 1
	set thr_mid = 69

#$ OPTION END

#$ OPTION BEGIN (UNCHECKED): LR Explorer/no cam - FLYWOO900HV 4S
    # -- ADDED: LR Explorer/no cam - FLYWOO900HV 4S --

	# failsafe
	rxfail 3 s 1425

	# gps
	set gps_rescue_throttle_min = 1250
	set gps_rescue_throttle_hover = 1418
	set gps_rescue_throttle_max = 1600

	# voltage
	set vbat_max_cell_voltage = 440
	set vbat_warning_cell_voltage = 345
	set vbat_min_cell_voltage = 335

	# scale
	set vbat_scale = 111
	set ibata_scale = 212

	# capacity
	set bat_capacity = 900
	set osd_cap_alarm = 700

	# rate
	rateprofile 0
	set thr_mid = 42
	rateprofile 1
	et thr_mid = 42

#$ OPTION END

#$ OPTION BEGIN (UNCHECKED): LR Explorer/no cam - GNB1300HV 4S
    # -- ADDED: LR Explorer/no cam - GNB1300HV 4S --

	# failsafe
	rxfail 3 s 1500

	# gps
	set gps_rescue_throttle_min = 1350
	set gps_rescue_throttle_hover = 1505
	set gps_rescue_throttle_max = 1700

	# voltage
	set vbat_max_cell_voltage = 440
	set vbat_warning_cell_voltage = 345
	set vbat_min_cell_voltage = 335

	# scale
	set vbat_scale = 111
	set ibata_scale = 212

	# capacity
	set bat_capacity = 1300
	set osd_cap_alarm = 1000

	# rate
	rateprofile 0
	set thr_mid = 51
	rateprofile 1
	set thr_mid = 51

#$ OPTION END


#$ OPTION BEGIN (UNCHECKED): LR Explorer/no cam - GNB1500HV 4S
    # -- ADDED: LR Explorer/no cam - GNB1500HV 4S --

	# failsafe
	rxfail 3 s 1525

	# gps
	set gps_rescue_throttle_min = 1400
	set gps_rescue_throttle_hover = 1530
	set gps_rescue_throttle_max = 1730

	# voltage
	set vbat_max_cell_voltage = 440
	set vbat_warning_cell_voltage = 345
	set vbat_min_cell_voltage = 335

	# scale
	set vbat_scale = 111
	set ibata_scale = 212

	# capacity
	set bat_capacity = 1500
	set osd_cap_alarm = 1200

	# rate
	rateprofile 0
	set thr_mid = 53
	rateprofile 1
	set thr_mid = 53

#$ OPTION END


#$ OPTION BEGIN (UNCHECKED): LR Explorer/no cam - GNB1350 4S
    # -- ADDED: LR Explorer/no cam - GNB1350 4S --

	# failsafe
	rxfail 3 s 1525

	# gps
	set gps_rescue_throttle_min = 1400
	set gps_rescue_throttle_hover = 1520
	set gps_rescue_throttle_max = 1700

	# voltage
	set vbat_max_cell_voltage = 430
	set vbat_warning_cell_voltage = 345
	set vbat_min_cell_voltage = 335

	# scale
	set vbat_scale = 111
	set ibata_scale = 212

	# capacity
	set bat_capacity = 1500
	set osd_cap_alarm = 1200

	# rate
	rateprofile 0
	set thr_mid = 52
	rateprofile 1
	set thr_mid = 52

#$ OPTION END


#$ OPTION BEGIN (UNCHECKED): CineStealth - Runcam - GNB1100HV 3S - 6blade
    # -- ADDED: CineStealth - Runcam - GNB1100HV 3S - 6blades --

	# failsafe
	rxfail 3 s 1325

	# gps
	set gps_rescue_throttle_min = 1200
	set gps_rescue_throttle_hover = 1320
	set gps_rescue_throttle_max = 1500

	# voltage
	set vbat_max_cell_voltage = 440
	set vbat_warning_cell_voltage = 345
	set vbat_min_cell_voltage = 335

	# capacity
	set bat_capacity = 1100
	set osd_cap_alarm = 900

	# rate
	rateprofile 0
	set thr_mid = 33
	rateprofile 1
	set thr_mid = 33

#$ OPTION END

#$ OPTION BEGIN (UNCHECKED): CineStealth - Runcam - GNB1100HV 3S - 3blade
    # -- ADDED: CineStealth - Runcam - GNB1100HV 3S - 3blades --

	# failsafe
	rxfail 3 s 1525

	# gps
	set gps_rescue_throttle_min = 1300
	set gps_rescue_throttle_hover = 1510
	set gps_rescue_throttle_max = 1650

	# voltage
	set vbat_max_cell_voltage = 440
	set vbat_warning_cell_voltage = 345
	set vbat_min_cell_voltage = 335

	# capacity
	set bat_capacity = 1100
	set osd_cap_alarm = 900

	# rate
	rateprofile 0
	set thr_mid = 51
	rateprofile 1
	set thr_mid = 51

#$ OPTION END


#$ OPTION BEGIN (UNCHECKED): SithLord - FlyWoo750MAhHV 2S - 2blade
    # -- ADDED: SithLord - FlyWoo750MAhHV 2S - 2blade --

	# failsafe
	rxfail 3 s 1400

	# gps
	set gps_rescue_throttle_min = 1200
	set gps_rescue_throttle_hover = 1382
	set gps_rescue_throttle_max = 1600

	# voltage
	set vbat_max_cell_voltage = 440
	set vbat_warning_cell_voltage = 345
	set vbat_min_cell_voltage = 335

	# capacity
	set bat_capacity = 750
	set osd_cap_alarm = 600

	# rate
	rateprofile 0
	set thr_mid = 39
	rateprofile 1
	set thr_mid = 39

#$ OPTION END

#$ OPTION BEGIN (UNCHECKED): SithLord - FlyWoo750MAhHV 2S - 3blade
    # -- ADDED: SithLord - FlyWoo750MAhHV 2S - 3blade --

	# failsafe
	rxfail 3 s 1375

	# gps
	set gps_rescue_throttle_min = 1200
	set gps_rescue_throttle_hover = 1372
	set gps_rescue_throttle_max = 1600

	# voltage
	set vbat_max_cell_voltage = 440
	set vbat_warning_cell_voltage = 345
	set vbat_min_cell_voltage = 335

	# capacity
	set bat_capacity = 750
	set osd_cap_alarm = 600

	# rate
	rateprofile 0
	set thr_mid = 38
	rateprofile 1
	set thr_mid = 38

#$ OPTION END

#$ OPTION BEGIN (UNCHECKED): SithLord - FlyWoo750MAhHV 2S - 5blade
    # -- ADDED: SithLord - FlyWoo750MAhHV 2S - 5blade --

	# failsafe
	rxfail 3 s 1250

	# gps
	set gps_rescue_throttle_min = 1150
	set gps_rescue_throttle_hover = 1235
	set gps_rescue_throttle_max = 1500

	# voltage
	set vbat_max_cell_voltage = 440
	set vbat_warning_cell_voltage = 345
	set vbat_min_cell_voltage = 335

	# capacity
	set bat_capacity = 750
	set osd_cap_alarm = 600

	# rate
	rateprofile 0
	set thr_mid = 25
	rateprofile 1
	set thr_mid = 25

#$ OPTION END

#$ OPTION BEGIN (UNCHECKED): SithLord - GNB850HV 3S - 3blade
    # -- ADDED: StealthTurtle - GNB850HV 3S - 3blade --

	# failsafe
	rxfail 3 s 1350

	# gps
	set gps_rescue_throttle_min = 1200
	set gps_rescue_throttle_hover = 1356
	set gps_rescue_throttle_max = 1600

	# voltage
	set vbat_max_cell_voltage = 440
	set vbat_warning_cell_voltage = 345
	set vbat_min_cell_voltage = 335

	# capacity
	set bat_capacity = 850
	set osd_cap_alarm = 700

	# rate
	rateprofile 0
	set thr_mid = 36
	rateprofile 1
	set thr_mid = 36

#$ OPTION END



#$ OPTION BEGIN (UNCHECKED): NoNameRacer - GNB 850Mah HV 3S - 3blade
    # -- ADDED: NoNameRacer - GNB 850Mah HV 3S - 3blade --

	# failsafe
	rxfail 3 s 1275

	# gps
	set gps_rescue_throttle_min = 1100
	set gps_rescue_throttle_hover = 1259
	set gps_rescue_throttle_max = 1500

	# voltage
	set vbat_max_cell_voltage = 440
	set vbat_warning_cell_voltage = 345
	set vbat_min_cell_voltage = 335

	# capacity
	set bat_capacity = 850
	set osd_cap_alarm = 700

	# rate
	rateprofile 0
	set thr_mid = 27
	rateprofile 1
	set thr_mid = 27

#$ OPTION END

#$ OPTION BEGIN (UNCHECKED): REKON 6/GP9 - FLYWOO3000LION 4S - 3 Blade
    # -- ADDED: REKON 6/GP9 - FLYWOO3000LION 4S - 3 Blade --

	# failsafe
	rxfail 3 s 1450

	# gps
	set gps_rescue_throttle_min = 1300
	set gps_rescue_throttle_hover = 1450
	set gps_rescue_throttle_max = 1650

	# voltage
	set vbat_max_cell_voltage = 422
	set vbat_warning_cell_voltage = 300
	set vbat_min_cell_voltage = 280

	# scale
	set vbat_scale = 111
	set ibata_scale = 213

	# capacity
	set bat_capacity = 2800
	set osd_cap_alarm = 2100

	# rate
	rateprofile 0
	set thr_mid = 45
	rateprofile 1
	set thr_mid = 45

#$ OPTION END

#$ OPTION BEGIN (UNCHECKED): REKON 6/GP9 - GNB1500HV 4S - 3 Blade
    # -- ADDED: REKON 6/GP9 - GNB1500HV 4S - 3 Blade --

	# failsafe
	rxfail 3 s 1450

	# gps
	set gps_rescue_throttle_min = 1300
	set gps_rescue_throttle_hover = 1450
	set gps_rescue_throttle_max = 1650

	# voltage
	set vbat_max_cell_voltage = 440
	set vbat_warning_cell_voltage = 345
	set vbat_min_cell_voltage = 335

	# scale
	set vbat_scale = 111
	set ibata_scale = 213

	# capacity
	set bat_capacity = 1500
	set osd_cap_alarm = 1250

	# rate
	rateprofile 0
	set thr_mid = 45
	rateprofile 1
	set thr_mid = 45

#$ OPTION END